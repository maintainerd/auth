# Environment Variables

The full, code-generated environment-variable reference — every variable, whether
it is required, its default, and production notes (TLS, secret providers, control
plane) — lives in a single source of truth:

➡️ **[`docs/contributing/environment-variables.md`](../../contributing/environment-variables.md)**

This file used to carry a second, hand-maintained copy that drifted out of sync
with the code (it listed removed variables as required and omitted ~25 real ones).
It has been collapsed into this pointer so there is exactly one list to keep correct.
