# maintainerd-auth — v1.0.0 Release Scope

This document defines what ships in **v1.0.0** — not an MVP, but a complete, production-grade identity server that can stand alongside Keycloak, Zitadel, and Auth0.

The goal is a shippable product that teams can self-host, extend, and build on with confidence. All enhancements, refactors, and additions after this baseline are tracked in subsequent releases.

---

## Repository Map

| Repo | Purpose |
|------|---------|
| `maintainerd-auth` | Identity server — REST API (this repo) |
| `maintainerd-auth-console` | Admin console UI (separate repo) |
| `maintainerd-auth-identity` | End-user identity portal — account management, OAuth2 consent, login UI (separate repo) |

All backend APIs required to drive both frontend repos are part of this v1.0.0 scope.

---

## Status Legend

| Symbol | Meaning |
|--------|---------|
| ✅ | Already implemented |
| 🔴 | Critical — must fix before release (security hole) |
| 🔨 | To build for v1.0.0 |
| 📅 | Deferred — v2.0.0 |
| ❌ | Out of scope |

---

## 1. Core Authentication

| Feature | Status |
|---------|--------|
| Username / email + password login | ✅ |
| Internal login (port 8080, no client_id) | ✅ |
| Public login (port 8081, client_id + provider_id) | ✅ |
| Logout (clears cookies + revokes session) | ✅ |
| User registration | ✅ |
| Configurable signup flows with role assignment | ✅ |
| Forgot password (token issuance + email) | ✅ |
| Reset password (token consumption) | ✅ |
| Email verification on signup (6-digit OTP, 1h TTL) | ✅ |
| Magic link / passwordless email login (15min, single-use) | ✅ |
| Invite flow with role pre-assignment | ✅ |
| Initial bootstrap / setup flow | ✅ |
| Force-password-change on next login flag | ✅ |
| Username / email change with re-verification | ✅ |
| Account deletion (GDPR right to erasure) | ✅ |
| Account data export (GDPR right to portability) | ✅ |
| SMS one-time code login | ✅ |
| Account recovery via backup codes | ✅ |

---

## 2. OAuth 2.0 / OIDC

### Endpoints

| Endpoint | Status |
|----------|--------|
| `POST /oauth/token` (RFC 6749) | ✅ |
| `GET /oauth/authorize` (RFC 6749) | ✅ |
| `POST /oauth/revoke` (RFC 7009) | ✅ |
| `POST /oauth/introspect` (RFC 7662, management port) | ✅ |
| `GET /oauth/userinfo` (OIDC Core 5.3) | ✅ |
| `GET /.well-known/openid-configuration` (OIDC Discovery) | ✅ |
| `GET /.well-known/oauth-authorization-server` (RFC 8414) | ✅ |
| `GET /.well-known/jwks.json` (RFC 7517) | ✅ |
| Consent challenge + decision endpoints | ✅ |
| List & revoke consent grants per user | ✅ |
| `POST /oauth/par` — Pushed Authorization Requests (RFC 9126) | ✅ |
| `POST /oauth/device_authorization` — Device Authorization (RFC 8628) | ✅ |
| `POST /oauth/register` — Dynamic Client Registration (RFC 7591) | ✅ |
| Token Exchange grant (RFC 8693) | ✅ |
| Back-Channel Logout (OIDC Back-Channel Logout 1.0) | ✅ |
| RP-Initiated Logout `/oauth/end_session` (OIDC Session Mgmt) | ✅ |
| CIBA — Client-Initiated Backchannel Authentication (poll mode) | ✅ |

### Grants

| Grant | Status |
|-------|--------|
| `authorization_code` with PKCE S256 (RFC 7636) | ✅ |
| `refresh_token` with rotation + family revocation | ✅ |
| `client_credentials` | ✅ |
| `device_code` (RFC 8628) | ✅ |
| `token-exchange` (RFC 8693) | ✅ |

### Client Model

| Feature | Status |
|---------|--------|
| Confidential / public client types | ✅ |
| `token_endpoint_auth_method`: basic, post, none | ✅ |
| `grant_types` and `response_types` arrays | ✅ |
| Per-client access / refresh token TTL override | ✅ |
| `require_consent` flag | ✅ |
| Client URIs (redirect, logout, logo, policy, tos) | ✅ |
| **Hash client secrets at rest** | ✅ |
| **Constant-time secret comparison (`crypto/subtle`)** | ✅ |
| Show client secret only once at creation | ✅ |
| Client secret rotation API (new + grace window) | ✅ |
| Per-client allowed scopes list | ✅ |
| Per-client allowed grant type enforcement at token endpoint | ✅ |
| `private_key_jwt` and `client_secret_jwt` auth methods (RFC 7523) | ✅ |
| mTLS client auth — `tls_client_auth` (RFC 8705) | ✅ |

### Tokens

| Feature | Status |
|---------|--------|
| Access token (JWT, RS256, 15 min) | ✅ |
| ID token with profile claims and nonce | ✅ |
| Refresh token (random, hashed at rest, 7 days, rotated) | ✅ |
| Authorization code (random, hashed at rest, single-use) | ✅ |
| `jti` claim with high entropy | ✅ |
| `kid` header for key rotation | ✅ |
| Algorithm-confusion attack protection (RS256 enforced) | ✅ |
| Access token denylist (Redis, keyed by `jti`) | ✅ |
| DPoP-bound access / refresh tokens (RFC 9449) | ✅ |

### Scopes & Claims

| Feature | Status |
|---------|--------|
| Standard OIDC scopes: `openid`, `profile`, `email`, `offline_access` | ✅ |
| Standard OIDC claims (sub, email, name, picture, address, phone) | ✅ |
| Scope-to-claim mapping per client | ✅ |
| Custom claim mappers (per tenant / client) | ✅ |

### Authorization Endpoint Hardening

| Feature | Status |
|---------|--------|
| PKCE required for `authorization_code` | ✅ |
| Redirect URI exact-match validation | ✅ |
| Authorization code single-use enforcement | ✅ |
| `state` parameter requirement enforcement | ✅ |
| `nonce` requirement when `response_type` includes `id_token` | ✅ |

---

## 3. Multi-Factor Authentication (MFA)

| Feature | Status |
|---------|--------|
| Email OTP utility (`internal/crypto/otp.go`) | ✅ |
| TOTP (RFC 6238) enrollment + verification | 🔨 |
| TOTP recovery / backup codes (one-time use) | 🔨 |
| WebAuthn / FIDO2 (passkeys) registration | 🔨 |
| WebAuthn login / 2FA assertion | 🔨 |
| SMS OTP (with rate-limit + cost guard) | 🔨 |
| Step-up authentication (re-auth for sensitive ops) | 🔨 |
| `acr_values` and `amr` claim support in tokens | 🔨 |
| Per-tenant MFA policy (required / optional) | 🔨 |
| MFA reset flow (admin-approved or recovery code) | 🔨 |

---

## 4. Federation & External Identity Providers

| Feature                                                            | Status |
| ------------------------------------------------------------------ | ------ |
| IdentityProvider model + repository + service                      | ✅      |
| Per-tenant provider configuration                                  | ✅      |
| User identity linking (`user_identity` model)                      | ✅      |
| OIDC upstream connector (Google, Microsoft, Apple, GitHub, GitLab) | 🔨     |
| Generic OAuth2 upstream connector                                  | 🔨     |
| Identity linking flow (API) for existing users                     | 🔨     |
| Identity unlinking                                                 | 🔨     |
| Just-in-time user provisioning from upstream IdP                   | 🔨     |
| Attribute mapping (upstream → local user fields)                   | 🔨     |
| Home-realm discovery (HRD) by email domain                         | 🔨     |
| SAML 2.0 SP (Service Provider)                                     | 🔨     |
| SAML 2.0 IdP-initiated SSO                                         | 🔨     |
| LDAP / Active Directory bind                                       | 📅     |
| Kerberos / SPNEGO                                                  | 📅     |

---

## 5. Provisioning (SCIM)

| Feature | Status |
|---------|--------|
| SCIM 2.0 `/Users` resource (RFC 7644) | 🔨 |
| SCIM 2.0 `/Groups` resource | 🔨 |
| SCIM 2.0 `/ServiceProviderConfig`, `/ResourceTypes`, `/Schemas` | 🔨 |
| SCIM 2.0 `PATCH` operations | 🔨 |
| SCIM 2.0 bulk endpoint | 🔨 |
| SCIM Bearer-token client authentication | 🔨 |
| Outbound SCIM push to downstream apps | 📅 |

---

## 6. Tenancy, Organizations & RBAC

| Feature | Status |
|---------|--------|
| Multi-tenant `Tenant` model | ✅ |
| `TenantMember` for user-to-tenant membership | ✅ |
| `TenantService` and `TenantSetting` per tenant | ✅ |
| `Role` and `UserRole` (many-to-many) | ✅ |
| `Permission` model with role permission mapping | ✅ |
| `Policy` and `ServicePolicy` | ✅ |
| Permission middleware | ✅ |
| User-context middleware joins JWT to DB user | ✅ |
| API key model with API / permission scoping | ✅ |
| Invite system with role pre-assignment | ✅ |
| Setup / bootstrap flow for first-run | ✅ |
| Per-tenant feature flags | ✅ |
| Tenant isolation invariant tests (cross-tenant access denied) | 🔨 |
| Tenant deletion with cascade / soft-delete + retention | 🔨 |
| Group model (separate from role, for human grouping) | 📅 |
| ABAC (attribute-based) policy evaluation | 📅 |
| Hierarchical orgs / sub-organizations | 📅 |

---

## 7. Session Management

| Feature | Status |
|---------|--------|
| User-token model (`user_token`) for refresh tracking | ✅ |
| Refresh token rotation with family | ✅ |
| Refresh token reuse detection → family revocation | ✅ |
| Cookie-based session (HTTP-only, `access_token` cookie) | ✅ |
| User-context cache invalidation | ✅ |
| Concurrent session limit | ✅ |
| List active sessions per user | 🔨 |
| Revoke single session by ID | 🔨 |
| Revoke-all-sessions endpoint | 🔨 |
| Session revoked on password change | 🔨 |
| Session revoked on permission change | 🔨 |
| Enforce `MaxConcurrentSessions` in login flow | 🔨 |
| Idle session timeout (sliding) | 🔨 |
| Absolute session lifetime cap | 🔨 |

---

## 8. Cryptography & Key Management

| Feature | Status |
|---------|--------|
| RSA-2048 minimum key strength enforced | ✅ |
| Key-pair consistency check at startup | ✅ |
| RS256 signing only (algorithm-confusion-safe) | ✅ |
| `kid` in JWT header | ✅ |
| Crypto-secure random for OTP, JTI, IDs | ✅ |
| PKCE S256 implementation | ✅ |
| SHA-256 hashing for refresh tokens and auth codes | ✅ |
| Bcrypt for password hashing | ✅ |
| Pre-computed dummy bcrypt hash for timing-safe ops | ✅ |
| **Propagate request `ctx` into `HashPassword` span** | 🔴 |
| Bcrypt cost ≥ 12 (currently `DefaultCost` = 10) | 🔨 |
| Argon2id support as configurable KDF | 🔨 |
| Multi-key JWKS (active + retiring keys) | 🔨 |
| Automatic key rotation runner (configurable period) | 🔨 |
| KMS-backed signing (AWS KMS / GCP KMS / Azure Key Vault) | 🔨 |
| ECDSA (ES256) and EdDSA support alongside RS256 | 🔨 |

---

## 9. Secret Management

| Feature | Status |
|---------|--------|
| Pluggable secret-provider abstraction | ✅ |
| Environment-variable provider | ✅ |
| AWS SSM Parameter Store provider | ✅ |
| AWS Secrets Manager provider | ✅ |
| HashiCorp Vault provider | ✅ |
| Azure Key Vault provider | ✅ |
| GCP Secret Manager provider | ✅ |
| Secret refresh / hot-reload without restart | 🔨 |
| Local file provider (Docker / K8s mounted secret) | 🔨 |

---

## 10. Password & Credential Policy

| Feature | Status |
|---------|--------|
| Minimum length 8, maximum 128 | ✅ |
| Requires upper, lower, digit, special character | ✅ |
| Common-password blocklist | ✅ |
| Configurable password policy per tenant | 🔨 |
| Password breach check via HIBP k-anonymity API | 🔨 |
| Password history (prevent last N reuse) | 🔨 |
| Password expiration / forced rotation policy | 🔨 |
| Disposable-email blocklist on signup | 🔨 |

---

## 11. Security Hardening

| Feature | Status |
|---------|--------|
| Brute-force protection on login | ✅ |
| Account lockout after N failed attempts | ✅ |
| Pre-computed dummy bcrypt to mask timing | ✅ |
| CORS middleware | ✅ |
| Security-headers middleware | ✅ |
| **CSRF protection on cookie-auth state-changing endpoints** | 🔴 |
| **IP-based rate limiting on `/login`, `/oauth/token`, `/forgot-password`, `/register`** | 🔴 |
| **Global request rate limiter (per IP) on public port 8081** | 🔴 |
| `Secure: true` enforced in production | 🔨 |
| `SameSite` configurable (Lax for redirect flows, Strict for management) | 🔨 |
| `__Host-` cookie prefix for session cookie | 🔨 |
| Separate refresh-token cookie (HTTP-only, `__Host-`, narrow path) | 🔨 |
| Per-environment CORS allow-list (no wildcard `*` with credentials) | 🔨 |
| `Strict-Transport-Security` (HSTS) with preload | 🔨 |
| `X-Content-Type-Options: nosniff` | 🔨 |
| `X-Frame-Options: DENY` | 🔨 |
| `Referrer-Policy: strict-origin-when-cross-origin` | 🔨 |
| `Strict Content-Security-Policy` for rendered HTML (login/consent pages) | 🔨 |
| Distributed rate limiter (Redis-backed, sliding window) | 🔨 |
| Per-client rate limits on `/oauth/token` | 🔨 |
| CAPTCHA / Turnstile after N failures | 🔨 |
| Maximum body size middleware on all endpoints | 🔨 |
| Strict `Content-Type` enforcement | 🔨 |
| URI / redirect validation rejecting `javascript:`, `data:` schemes | 🔨 |

---

## 12. Observability

### Logging

| Feature | Status |
|---------|--------|
| Structured logging | ✅ |
| Request ID middleware | ✅ |
| Recovery middleware with stack capture | ✅ |
| Trace-correlated log fields | ✅ |
| PII redaction layer before log output | 🔨 |
| Per-environment log level via config | 🔨 |

### Metrics

| Feature | Status |
|---------|--------|
| OpenTelemetry meter provider | ✅ |
| **HTTP server metrics via `otelhttp`** | 🔴 |
| Auth-specific counters (logins ok/fail, tokens issued, MFA challenges) | 🔨 |
| Database query duration histogram | 🔨 |
| Cache hit/miss counters | 🔨 |
| Prometheus `/metrics` endpoint on management port | 🔨 |
| Build-info gauge (version, commit, date) | 🔨 |

### Tracing

| Feature | Status |
|---------|--------|
| OpenTelemetry tracer provider with OTLP exporter | ✅ |
| Spans on service-layer operations | ✅ |
| Spans on JWT generation/validation | ✅ |
| Spans on bcrypt password operations | ✅ |
| GORM tracing instrumentation | ✅ |
| **HTTP server middleware via `otelhttp.NewHandler`** | 🔴 |
| Outbound HTTP client instrumentation (federation, webhooks) | 🔨 |
| Redis client instrumentation | 🔨 |

---

## 13. Audit Logging

| Feature | Status |
|---------|--------|
| Audit-log model and repository | ✅ |
| Auth-event model (login success/failure, token issued, etc.) | ✅ |
| Retention runner | ✅ |
| Recording on login success, failure, lockout | ✅ |
| Audit every privileged admin action (user/role/client CRUD) | 🔨 |
| Audit consent grant / revoke / token revoke | 🔨 |
| Append-only storage (no UPDATE/DELETE on audit tables) | 🔨 |
| Per-tenant audit isolation | 🔨 |
| PII redaction policy in audit payloads | 🔨 |

---

## 14. Database & Migrations

| Feature | Status |
|---------|--------|
| PostgreSQL via GORM | ✅ |
| Auto-migrate runner | ✅ |
| Seed runner | ✅ |
| Generic base repository | ✅ |
| Soft-delete + audit timestamps via `model.Base` | ✅ |
| OTEL-instrumented driver | ✅ |
| Versioned migrations tool (golang-migrate / goose) instead of GORM auto-migrate in prod | 🔨 |
| Forward + rollback migration scripts | 🔨 |
| Connection-pool tuning (max open, max idle, lifetime) | 🔨 |
| Database SSL/TLS required in production | 🔨 |
| Indexes audited (email, username, client_id, provider_id) | 🔨 |
| Statement timeout enforced (`SET statement_timeout`) | 🔨 |
| Backup + point-in-time recovery procedure documented | 🔨 |

---

## 15. Cache (Redis)

| Feature | Status |
|---------|--------|
| Redis client with TLS support | ✅ |
| User-context cache invalidation | ✅ |
| Cache abstraction interface | ✅ |
| Redis OTEL instrumentation (spans + metrics) | 🔨 |
| Sentinel / Cluster support for HA | 🔨 |
| Circuit breaker around cache reads (degrade gracefully) | 🔨 |

---

## 16. Email / SMS / Notifications

| Feature | Status |
|---------|--------|
| SMTP email service | ✅ |
| Email templates (forgot password, invite) | ✅ |
| Pluggable email provider (SES / SendGrid / Postmark / Mailgun / Resend) | 🔨 |
| Async email delivery via queue (avoid blocking auth flows) | 🔨 |
| Email delivery retry with backoff | 🔨 |
| SMS provider abstraction (Twilio / SNS / Vonage) | 🔨 |
| Localized email templates (i18n) | 🔨 |
| Email sandbox mode for development | 🔨 |

---

## 17. Webhooks

| Feature | Status |
|---------|--------|
| Webhook endpoint model + CRUD API | ✅ |
| Outbound webhook delivery for auth events | 🔨 |
| HMAC-SHA256 signature header for webhook authenticity | 🔨 |
| Replay protection (timestamp + tolerance window) | 🔨 |
| Retries with exponential backoff + dead-letter queue | 🔨 |
| Per-tenant webhook configuration | 🔨 |
| Event-type subscription model | 🔨 |

---

## 18. REST API Quality

| Feature | Status |
|---------|--------|
| Versioned route mounting | ✅ |
| Consistent JSON error envelope | ✅ |
| OAuth-spec-compliant error format | ✅ |
| Standard middleware: logger, recovery, request ID | ✅ |
| OpenAPI 3.1 spec generated and served on `/openapi.json` | 🔨 |
| Swagger UI / Redoc on management port | 🔨 |
| Pagination, sorting, filtering conventions applied consistently | 🔨 |
| ETag / `If-None-Match` for cacheable resources (jwks, discovery) | 🔨 |
| Problem Details (RFC 7807) response shape for non-OAuth errors | 🔨 |

---

## 19. Configuration & Environment

| Feature | Status |
|---------|--------|
| Env-driven config | ✅ |
| Secret-manager-driven config | ✅ |
| Per-port config (management vs identity) | ✅ |
| Single canonical `Config` struct (no package-level globals) | 🔨 |
| Validation of required env at boot (fail-fast) | 🔨 |
| `.env.example` kept in sync with code | 🔨 |

---

## 20. Health & Readiness

| Feature | Status |
|---------|--------|
| `/healthz` endpoint | ✅ |
| `/readyz` (checks DB + Redis + JWKS loaded) | 🔨 |
| `/livez` (process-level only) | 🔨 |
| Health check JSON includes version + dependency status | 🔨 |

---

## 21. Deployment & Operations

| Feature | Status |
|---------|--------|
| Dockerfile | ✅ |
| Distroless / minimal base image | 🔨 |
| Non-root container user | 🔨 |
| Read-only root filesystem | 🔨 |
| Kubernetes manifests / Helm chart | 🔨 |
| Graceful shutdown on SIGTERM | 🔨 |
| NetworkPolicy isolating management port from public ingress | 🔨 |
| Image signing (cosign / sigstore) | 🔨 |
| Vulnerability scanning in CI (trivy / grype) | 🔨 |

---

## 22. Testing

| Feature | Status |
|---------|--------|
| Unit tests for `apperror` | ✅ |
| Unit tests for `secret_manager` providers | ✅ |
| Unit tests for `valid` package | ✅ |
| Unit tests for selected services | ✅ |
| ≥ 70% coverage on `internal/service/` | 🔨 |
| ≥ 70% coverage on `internal/security/` | 🔨 |
| Integration tests with real Postgres + Redis (testcontainers-go) | 🔨 |
| End-to-end OAuth flow tests (authorize → token → introspect → revoke) | 🔨 |
| Fuzz tests on token parsing, redirect-URI validation, PKCE verifier | 🔨 |
| Race-detector enabled in CI (`go test -race`) | 🔨 |
| Conformance test against OpenID Foundation test suite | 🔨 |

---

## 23. CI / Code Quality

| Feature | Status |
|---------|--------|
| Go 1.26.x toolchain | ✅ |
| Errors wrapped with context | ✅ |
| `golangci-lint` strict config in CI | 🔨 |
| `gosec` clean (no high/medium findings) | 🔨 |
| `go vet` and `staticcheck` clean | 🔨 |
| Context as first parameter on every I/O-bound function | 🔨 |
| No `context.Background()` in request-scoped paths | 🔨 |
| Secret scanning: gitleaks pre-commit + CI | 🔨 |
| Dependency review automation (Dependabot / Renovate) | 🔨 |
| Static analysis: CodeQL workflow | 🔨 |

---

## 24. Documentation

| Feature | Status |
|---------|--------|
| README with quick-start | ✅ |
| Architecture doc | ✅ |
| Contributor guide | ✅ |
| `CODE_OF_CONDUCT.md` | ✅ |
| Full REST API reference (`docs/apis/`) | ✅ |
| `SECURITY.md` (threat model, reporting, supported versions) | 🔨 |
| OpenAPI spec (machine-readable) for both ports | 🔨 |
| Operator runbook (deploy, rotate keys, scale, recover) | 🔨 |
| Migration guide from Keycloak / Auth0 / Zitadel | 🔨 |
| Changelog (Keep-A-Changelog format) | 🔨 |
| Public docs site (docusaurus / mkdocs) with versioned releases | 🔨 |

---

## Deferred — v2.0.0

These are real features that belong in the product but are not required for a complete v1.0.0.

| Feature | Reason |
|---------|--------|
| LDAP / Active Directory bind | High complexity; OIDC federation covers most enterprise SSO needs |
| Kerberos / SPNEGO | Narrow enterprise use case |
| ABAC (attribute-based access control) | RBAC + policies cover v1 use cases |
| Hierarchical orgs / sub-organizations | Complex data model change, additive in v2 |
| Group model (separate from role) | Additive, non-breaking |
| Outbound SCIM push to downstream apps | Inbound SCIM sufficient for v1 |
| Risk-based / adaptive MFA | Requires ML pipeline or third-party risk engine |
| Geo / IP anomaly detection | Requires data enrichment infrastructure |
| Device fingerprinting + trusted-device management | Complex, additive |
| Pairwise subject identifiers | Privacy feature, additive |
| Encrypted ID tokens (JWE) | Rare requirement, additive |
| Resource indicators (RFC 8707) | Additive, no breaking changes |
| FAPI 2.0 full conformance | Financial-grade, narrow audience |
| JAR (signed Request Objects, RFC 9101) | Additive |
| JARM (signed Response Mode) | Additive |
| Streaming SIEM export (Kafka / Kinesis / S3) | Ops feature, additive |
| Tenant export / clone / migrate | Additive ops tool |
| KMS HSM (PKCS#11) backend | Rare on-prem requirement |
| Blue/green deploy strategy | Operational docs |
| Terraform / Pulumi modules | Infrastructure tooling |
| SOC 2 Type II audit readiness | Operational / process work |
| ISO 27001 certification | Operational / process work |
| Per-feature how-to guides | Documentation enhancement |
| Video tutorials | Documentation enhancement |

---

## 25. gRPC

gRPC surface mirrors the REST API, exposing the same identity operations over a high-performance binary protocol. Useful for service-to-service auth (token introspection, user lookup) inside a cluster without HTTP overhead.

| Feature | Status |
|---------|--------|
| Proto definitions (`api/proto/`) for core services | 🔨 |
| Generated Go stubs build target | 🔨 |
| gRPC server on dedicated port (management-only by default) | 🔨 |
| gRPC reflection on management port | 🔨 |
| Auth interceptor (mirrors JWT middleware) | 🔨 |
| Logging interceptor (mirrors request-ID / structured log middleware) | 🔨 |
| Recovery interceptor (mirrors panic recovery middleware) | 🔨 |
| Tracing interceptor (OpenTelemetry, mirrors `otelhttp`) | 🔨 |
| gRPC health-check service (`grpc.health.v1`) | 🔨 |
| gRPC-Gateway transcoding to REST (optional, dual surface) | 🔨 |

---

## Out of Scope

| Feature | Reason |
|---------|--------|
| Admin console UI | `maintainerd-auth-console` repo |
| End-user identity portal UI | `maintainerd-auth-identity` repo |
| PCI-DSS compliance | Auth server should never touch cardholder data |
| HIPAA BAA | Operational, not a code feature |
| Dark mode | Frontend concern |
| Biometric preference flag | Frontend / device concern |

---

## v1.0.0 Critical Path (Do First)

Before any new feature work, these six security issues must be closed. They are holes, not gaps.

| # | Item | Section |
|---|------|---------|
| 1 | Hash client secrets at rest + constant-time compare | §2 |
| 2 | CSRF protection on cookie-auth state-changing endpoints | §11 |
| 3 | IP-based rate limiting on `/login`, `/oauth/token`, `/register` | §11 |
| 4 | `otelhttp.NewHandler` HTTP server instrumentation | §12 |
| 5 | Propagate request `ctx` into `HashPassword` | §8 |
| 6 | CORS allow-list (no wildcard `*` with credentials) + HSTS | §11 |
