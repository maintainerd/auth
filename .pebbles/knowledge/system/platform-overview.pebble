{
  "content": {
    "body": "Pebbles is a spec-graph IDE for AI-assisted development. A project is\nspecified by PEBBLES — small linked JSON files (`.pebble`) under\n`.pebbles/{section}/` — so humans and AI agents navigate intent\nsurgically instead of re-reading the whole codebase. Statuses track\nspec↔code alignment; the graph makes impact and traceability computable.\n\n## The 13 sections\n\n| Section | Holds |\n|---|---|\n| workflows | THE MAIN section, labeled \"Application\" — the whole app's structure & entry points as flow graphs (nodes, edges, sub-processes), plus checklists (requirement gates) and text notes |\n| models | database tables: fields with types, PK/FK, nullability, defaults, plus named relationships (has-one/has-many/belongs-to/… to referenced models) |\n| structures | in-code shapes: DTOs, payloads, configs, wire request/response bodies (code types) |\n| api | API endpoint definitions, ANY protocol (REST/GraphQL/gRPC/WebSocket/SSE/SOAP/webhook): operation, path, params, headers, request/response payloads |\n| queries | visual queries (view-only spec): a base Model + JOINs on fields + preloaded relationships + filters, previewed as SQL in a chosen dialect |\n| rules | prescriptive constraints with enforcement levels (strict/recommended/optional) |\n| architecture | allowed dependencies, layering, boundaries |\n| tests | test PLANS: one pebble per feature, one checklist item per case |\n| findings | dated evidence reports (bugs AND pass proofs) |\n| skills | reusable AI procedures — become /slash-commands in chat |\n| agents | persona/agent definitions: who an agent is, tools, boundaries |\n| memory | durable facts exported from chat sessions |\n| knowledge | shared context library (this document's section) |\n\nSystem records live in a `system/` subfolder of skills, rules,\nknowledge and agents: pinned, edit-only, non-deletable, self-healing,\nrestorable.\n\n## Models vs structures vs APIs — never blur these\n\nThree different things:\n\n- **model** = a PERSISTED database table. Truth comes from the\n  DDL/migration. Carries DB semantics: primaryKey, refKey (FKs),\n  required = NOT NULL (shown as Nullable), default verbatim from the\n  schema. DB column types (varchar, bigint, …).\n- **structure** = an IN-CODE shape: DTO, payload, config, return type,\n  or a wire request/response body. Truth comes from the source code.\n  Code types (string, number, SomeClass). NO DB semantics — a structure\n  has no primary key, no FK, no NOT NULL; it may nest other\n  models/structures via typeRef.\n- **api** = a WIRE-LEVEL endpoint definition: method/path, params,\n  headers, request and response payloads, status codes. Payload shapes\n  are NOT redefined inline: they REFERENCE structures via structureRef.\n\nClassification litmus: persisted → model · passed around in code →\nstructure · exposed over the wire as an endpoint → api. Extracting a DTO\nas a model (or a table as a structure) is a classification defect — file\nit and fix the section, don't force the fields to fit.\n\n## Pebble anatomy (top-level keys)\n\n`version` (schema version) · `id` (UUID, immutable, the canonical\nidentity) · `name` (display title) · `type` (matches the section) ·\n`description` · `tags[]` · `status` · `priority` or `enforcement`\n(rules) · `createdAt`/`updatedAt` (createdAt immutable) · `refs[]`\n(outgoing graph edges) · `codeLinks[]` (implementing files) ·\n`content` (the kind-specific payload).\n\n## Content kinds\n\n`checklist` (items with per-item status/description/refs) · `fields`\n(table columns) · `flow` (nodes + edges + sub-process links) · `text`\n(markdown body) · `skill` (purpose/whenToUse/steps/constraints/output/\nexamples) · `agent` · `memory` · `finding` (dated report).\n\n## Status vocabulary (the product itself)\n\nplanned (not built) · in-progress · implemented (exists in code) ·\nverified (behavior proven) · drift (code moved, spec stale) · broken\n(proven failing) · missing (spec claims it, code lacks it) · deprecated.\nStatuses exist at BOTH levels — the pebble and each unit (field, item,\nnode). Unit statuses are the feature checklist the user reads.\n\n## Reference types (sparse, load-bearing — see the consensus)\n\n- `refs[]`: labeled edges `{id, section, targetKind: \"pebble\"|\"field\"|\n  \"item\"|\"node\", pebbleId, targetId?, path, label}`\n- `refKey` (model field): FK → another model's field\n- `typeRef` (field): the type IS another model/structure\n- `subprocess` (workflow node): → another workflow\n- `codeLinks[]`: `{id, path, symbol?, line?, note?}` — path doubles as\n  a code-map node id\n- Backlinks are COMPUTED (`pebbles-cli refs <path>`), never stored.\n- Policy: an edge exists when following it changes what an agent builds\n  or lets a validator catch a break. No thematic \"related-to\" edges.\n\n## Files and storage\n\n`.pebble` files are JSON (legacy .json still reads + migrates).\n`.pebblesignore` at the project root (gitignore syntax) scopes the code\nmap. `PEBBLES.md` at the project root is the on-disk AI entry point;\n`pebbles-cli onboard` briefs CLAUDE.md/AGENTS.md for external agents.\nSpec (all sections) belongs to the repo and is git-shared. Personal data\nnever touches the repo: chats, run journal and window state live in the\nOS app-data dir; saved maps live under .pebbles/.maps (local churn).",
    "kind": "text"
  },
  "createdAt": "2026-07-21T14:32:38.650Z",
  "description": "The complete model: sections, pebble anatomy, content kinds, status vocabulary, reference types, file conventions, and what lives where.",
  "id": "0666d6bf-0891-49a1-93f8-b4cac16ecd57",
  "name": "Pebbles platform overview",
  "status": "verified",
  "tags": [
    "system"
  ],
  "type": "knowledge",
  "updatedAt": "2026-07-28T03:27:29.097Z",
  "version": 1
}
